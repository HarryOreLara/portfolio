import { FilterTypeSkillPipe } from './filter-type-skill.pipe';

describe('FilterTypeSkillPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTypeSkillPipe();
    expect(pipe).toBeTruthy();
  });
});
